name: xl_demo
description: A demonstration of several XL parallax widgets.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  very_good_analysis: ^1.0.4
  flutter_test:
    sdk: flutter
  xl:
    path: ../

flutter:
  uses-material-design: true
  assets:
    - res/
